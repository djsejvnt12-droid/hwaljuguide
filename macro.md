# 🎯 매크로 설정 가이드

이곳은 **활주 서버**를 위한 자동화 가이드입니다.

---

## 1. 주요 좌표값
좌표 설정은 아래 값을 참고하세요.
* **파란색 글자 좌표**: `[123, 456]`
* **인식 범위**: `[789, 101]`

> **⚠️ 주의사항**
> 좌표는 반드시 게임 해상도 설정에 맞춰야 정확하게 인식됩니다.

---

## 2. 방어 로직 (F1)
매크로 실행 중 오류가 발생하면 아래 로직이 작동합니다.

```python
# 실패 시 방어 로직
if 인식_실패 == True:
    press('f1')
    print("F1 키를 눌러 방어 기능을 실행합니다.")
